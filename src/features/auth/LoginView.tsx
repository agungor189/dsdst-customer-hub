import { useState } from "react";import { ArrowRight,LockKeyhole,MessagesSquare,ShieldCheck } from "lucide-react";import { api } from "../../lib/api";import type { PanelUser } from "../../../shared/contracts/domain";
export function LoginView({onLogin}:{onLogin:(user:PanelUser)=>void}){const [username,setUsername]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);return <main className="login-shell"><section className="login-brand"><div className="brand-mark"><MessagesSquare size={25}/></div><p className="eyebrow">DSDST OPERATIONS</p><h1>Müşteri konuşmaları,<br/><em>tek bir yerde.</em></h1><p>Kanallar arasında kaybolmadan müşterilerinize hızlı, tutarlı ve kişisel yanıtlar verin.</p><div className="security-note"><ShieldCheck size={17}/><span>Panel hesabınızla güvenli giriş</span></div></section><section className="login-card"><div><p className="eyebrow">CUSTOMER HUB</p><h2>Tekrar hoş geldiniz</h2><p>Devam etmek için Panel hesabınızı kullanın.</p></div><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError("");try{const r=await api<{user:PanelUser}>("/auth/login",{method:"POST",body:JSON.stringify({username,password})});onLogin(r.user);}catch(e:any){setError(e.message)}finally{setBusy(false)}}}><label>Kullanıcı adı veya e-posta<input value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username" autoFocus/></label><label>Şifre<div className="password-field"><LockKeyhole size={16}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></div></label>{error&&<p className="form-error">{error}</p>}<button className="primary-button" disabled={busy}>{busy?"Doğrulanıyor…":"Giriş yap"}<ArrowRight size={17}/></button></form><small>Oturum bilgisi tarayıcı kodundan erişilemeyen güvenli bir çerezde tutulur.</small></section></main>}
