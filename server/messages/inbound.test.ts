import test from"node:test";import assert from"node:assert/strict";import{testDatabase}from"../test-utils.js";import{ingestInbound}from"./inbound.js";
test("duplicate webhook and external message create one normalized message",()=>{const{db}=testDatabase();const input={eventId:"event-1",externalAccountId:"website",externalConversationId:"conv-1",externalMessageId:"message-1",externalUserId:"visitor-1",displayName:"Ada",body:"Merhaba",messageType:"TEXT",externalCreatedAt:new Date().toISOString(),metadata:{source:"test"}};const first=ingestInbound(db,"WEBSITE",input);const duplicate=ingestInbound(db,"WEBSITE",input);assert.equal(first.duplicate,false);assert.equal(duplicate.duplicate,true);assert.equal((db.prepare("SELECT count(*) count FROM messages WHERE external_message_id='message-1'").get()as any).count,1);db.close();});
