import fs from "node:fs";import path from "node:path";import Database from "better-sqlite3";import {loadConfig}from"../config.js";
const config=loadConfig();const backupDir=path.resolve(process.env.BACKUP_DIR??path.join(path.dirname(config.databasePath),"backups"));fs.mkdirSync(backupDir,{recursive:true});const stamp=new Date().toISOString().replace(/[:.]/g,"-");const target=path.join(backupDir,`customer-hub-${stamp}.db`);const db=new Database(config.databasePath);await db.backup(target);db.close();const check=new Database(target,{readonly:true});if(check.pragma("integrity_check",{simple:true})!=="ok"){check.close();throw new Error("Backup integrity check failed");}check.close();console.log(target);
